import { billCommittee } from 'legislator-lookup/helpers/bill-committee';
import { module, test } from 'qunit';

module('Unit | Helper | bill committee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = billCommittee([42]);
  assert.ok(result);
});
